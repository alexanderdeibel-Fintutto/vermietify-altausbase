import React from 'react';
import ContextualHelpTooltip from '@/components/help/ContextualHelpTooltip';

export default function HelpTooltip({ content }) {
  return <ContextualHelpTooltip content={content} />;
}